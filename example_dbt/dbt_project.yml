name: zed_dbt_example
version: 1.0.0
config-version: 2

profile: zed_dbt_example

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  zed_dbt_example:
    staging:
      +materialized: view
    marts:
      +materialized: table
